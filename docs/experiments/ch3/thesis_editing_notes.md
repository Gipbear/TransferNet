# 第三章编校与迁移备忘


> **初稿编校说明（不属于论文正文）**
>
> 1. 参考文献编号待合并论文全文后统一调整。
> 2. 当前不使用“通用于任意KGQA模型”“路径忠实性已得到证明”或“全面优于现有方法”等表述。
> 3. 指标口径：本章检索侧报PathHit@K、路径P、路径R和多样性，下游问答侧报Hit和Hit@1；Macro-P/R/F1、Micro系列和EM统一在第四章报告，合并全文时不要回填。路径检索指标不与外部结果合并。表3-4中的下游问答列取自第三章批次（`downstream_qa/transfernet_v2/base_zeroshot/full/`），第四章"零样本+路径"基线为另一次复现（Hit@1 82.29），合并全文时须统一为同一批次或加注说明。表3-2仅作背景参照，不作无条件方法排名。
> 4. 图3-1至图3-4均提供SVG可编辑源文件，统一存放于`docs/experiments/ch3/images/`；图3-5由`scripts/plot_ch3_param_sensitivity.py`生成；图3-6当前SVG为人工定稿的规范版本，案例证据见`data/analysis/20260722_2040__ch3_case_analysis/README.md`。`scripts/build_ch3_fig6_case_drawio.py`已同步文字口径，但运行后须重新进行视觉核对，不得直接覆盖定稿图。
> 5. 正文方法名为RSDPR，对应`experiment_results.md`及工程实验记录中的实验条件标识`TARRS`；实验记录不同步改名，核对结果时按此映射对应。消融配置的正文名称与实验记录标识的对应关系为：模型推理得分引导策略↔`终点感知Score-Beam`（`penalty_mode=none`，\(\beta=0,\eta=1\)）、固定加性惩罚↔`penalty_mode=fixed`、自适应冗余抑制策略↔`penalty_mode=adaptive`；正文两项策略的简称统一为"模型推理得分引导策略"（全称见第3.2.2节）和"自适应冗余抑制策略"（全称见第3.2.3节），均带"策略"后缀；作为对比方法的最短路径检索不带该后缀，以便与本章所提策略相区分。表3-6中"逐跳实体得分＋关系得分"一行↔`beam20_lambda02_eta0`。原得分项消融表中的`普通Score-Beam`（\(\beta=0,\eta=0\)，90.70）一行在调整消融顺序后已删除，其结论由现表3-6承担。表3-6四行均为\(\beta=0.2\)、`penalty_mode=adaptive`（见`experiment_results.md`第5.2节受控条件），故正文写明该组消融在已启用自适应冗余抑制的前提下进行，不可与表3-7混为同一维度。
> 6. 式（3-9）的MMR思想已补充参考文献[6]；合并全文时仅需随全篇参考文献顺序统一调整编号。工程代码中的`mmr_score`变量名与历史方法名`AP-MMR`不作为论文口径，正文不以MMR命名本章方法。
> 7. 原“检索效率与结果讨论”小节已删除；效率数据保留在`experiment_results.md`第8节，经确认不写入正文，本章不以效率作为证据。
> 8. 第3.3.4节的参数扫描已由三张表合并为图3-5，正文直接依据图中扫描点讨论趋势，不再引用尚未建立的附录A；完整数值保留于`experiment_results.md`第6.1~6.3节及第6.5节。
> 9. 正文引用的指标差值统一按表中两位小数相减给出，以便读者复算；`experiment_results.md`中由逐样本配对bootstrap计算的点估计与之存在0.01级差异（如PathHit@20的+0.70/+4.43、QA Hit@1的+3.16/+0.06），核对时以配对值为准，正文不再列出。
> 10. 第3.3.3节按"总—分"编排，依次为：表3-5是本章两项策略的累加式消融（首行为不含任何本章策略的最短路径检索，末行即RSDPR），表3-6拆解模型推理得分引导策略内部的得分项，表3-7对比关系冗余抑制的实现形式（对应`penalty_mode`的`none`/`fixed`/`adaptive`，三者互斥而非累加）。后两部分分别下挂在两项策略之下，不可合并回一张表。3.3.2节主实验使用合并表3-4：路径检索指标与下游问答指标同表，无路径行仅报告下游指标。全章3.2、3.3节内不设四级标题，分点一律用段首粗体引导词，且同一小节内不再嵌套第二层粗体标签。最短路径检索与固定加性惩罚（式3-9）均为对照形式，不是本章所提策略的一档，这一定位与第3.2.3节由式（3-9）的不足引出式（3-10）的行文一致。各表存在共用运行：表3-5第2行＝表3-7第1行（92.98/64.73/81.59、Hit 88.61），表3-5第3行＝表3-6末行＝表3-7第3行（94.43/71.79，Hit 90.01、Hit@1 82.23），表3-4的最短路径检索行与RSDPR行分别与表3-5首行、末行同源，任一处数值更新须同步其余表。表3-5不含PathHit@1列（该指标的三档同族对照见表3-7）；表3-5的D_rel列仅作后两行同族对照，正文讨论不作跨路径获取方式比较。第三章表数为7张，合并全文时须同步图表清单。
> 11. 表3-2的外部比较严格区分Hit与Hit@1：Hit判断全部预测答案中是否存在命中，Hit@1判断归一化后的首个预测答案是否命中；外部数值统一转引GNN-RAG对照表，历史口径错误及修订过程见`experiment_results.md`第11节P8。
> 12. RSDPR零样本条件共输出5,488个预测实体，平均每题约3.47个；预测集合规模会影响Hit，因而Hit与Hit@1之间7.78个百分点的差距不能仅解释为首答案排序误差。该统计用于答辩和口径说明，不写入第三章正文。

**待并入第二章的素材（不属于本章正文，在chapter5分支的第二章md中补写）：**

拟新增 2.5 实验数据集、2.6 评价指标、2.7 实验运行环境三节，供第三、四、五章共用；三章正文以“见第2.5节”等方式引用。以下内容自本章3.3.1移出。

*（1）2.5 实验数据集*

本文实验采用多跳知识图谱问答数据集WebQSP。该数据集以Freebase为知识图谱，问题样本包含自然语言问题、主题实体和答案实体标注，实际使用的数据划分及统计信息如下表所示。该数据集平均每个问题包含1.00个主题实体和10.47个去重答案实体；测试集中包含623个二跳问题，占测试集的39.41%，能够覆盖多跳推理场景。


| 数据集 | 训练集 | 测试集 | 平均主题实体数 | 平均答案数 | 测试集跳数分布   |
| ------ | -----: | -----: | -------------: | ---------: | ---------------- |
| WebQSP |   2996 |   1581 |           1.00 |      10.47 | 一跳958；二跳623 |

*（2）2.7 实验运行环境*


| 项目         | 配置                             |
| ------------ | -------------------------------- |
| 操作系统     | WSL2 Linux 6.18.33.2（x86_64）   |
| CPU          | Intel Core i5-12600KF            |
| GPU          | NVIDIA GeForce RTX 4060 Ti 16 GB |
| Python       | 3.12.12                          |
| PyTorch/CUDA | 2.10.0+cu128                     |
